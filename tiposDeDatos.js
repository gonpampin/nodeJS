/* Tipos de datos

--strings--numeros--buleanos--palabras reservadas--
*/

let verdadero = true

let nada = null

let texto = ' hola'

let numero = 23

//Numeros

//--positivos--negativos--decimales--

let positivo = Number (5)

let negativo = Number (-5)

let decimal = Number (0.5)

// Funcion nativa 

/**--parseInt()-- Esta nos devuelve el valor entero de un dato que le pasemos como parámetro,
 *  siempre y cuando el mismo se pueda operar como dato numérico.*/

const nan = parseInt('hola') // return = NaN

const correcto = parseInt(1.235) // return = 1